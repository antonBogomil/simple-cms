const styles = theme => ({
    managerPath: {
        padding: '20px'
    },

    folderContainer:{
        display: 'flex',
        flexDirection: 'row',
        flexFlow: 'row wrap'
    }
});

export default styles;